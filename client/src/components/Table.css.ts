import {style} from '@vanilla-extract/css';

export const tableStyles = style({
  tableLayout: 'fixed',
});

export const imageStyles = style({
  width: 30,
  aspectRatio: '1',
});

export const dataRowStyles = style({
  display: 'flex',
  alignItems: 'center',
});
